git init 
git config --global user.name "[name]"
git config --global user.email "[email address]"
git config --global color.ui auto


changes:
    stash                      隐藏
    discard                    丢弃
    stage      unstage         暂存（到暂存区index）             =   git add .          ===》 commit 

git commit                     提交到当前分支


fatal   致命问题
